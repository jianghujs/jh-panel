# 任务

- [x] **后端状态接口**
  - 在 `plugins/keepalived/index.py` 中新增获取 VIP 归属、keepalived 服务状态、日志内容的 API。
  - 校验：命令行调用接口能返回结构化 JSON，包含 VIP 是否在本机、服务状态、日志片段等字段。
- [x] **前端状态面板**
  - 更新 `index.html` 与 `js/keepalived.js`，新增“状态面板”菜单项，渲染类似 rsyncd 的实时状态 UI。
  - 校验：打开插件页面能看到状态面板，面板展示 VIP 状态、日志、其它指标，刷新按钮生效。
- [x] **交互与刷新**
  - 添加手动刷新与可选的轻量自动刷新（若必要），处理错误提示。
  - 校验：通过 UI 触发刷新时，数据更新、错误能提示。
