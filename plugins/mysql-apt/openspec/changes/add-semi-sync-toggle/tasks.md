## 1. 实现
- [x] 1.1 编写后端工具/接口：读取半同步状态、在 `my.cnf` 中添加/删除指定指令、重启 MySQL，并在主/从流程里设置 `rpl_semi_sync_*` 全局变量。
- [x] 1.2 更新主从配置页 UI（`index.html` + `js/mysql-apt.js`），展示半同步开关状态、提示确认，并能触发主/从 SQL 操作。
- [x] 1.3 将半同步能力接入从节点初始化流程：启用时自动先执行从节点 SQL 再返回成功。
- [x] 1.4 手动验证：切换开关、确认配置写入和重启日志，以及主/从 SQL 是否成功执行。
